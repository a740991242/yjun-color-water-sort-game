# Claude 入口说明

本项目的 AI 协作规范以 `AGENTS.md` 为准。

## 输出协议

默认全程静默。除非用户明确要求解释、总结、计划或列出改动，否则不要输出过程、改动说明、命令日志或验证详情。任务成功完成后最终回复只能是“已完成”。任务未完成时，只用一句话说明卡在哪一步和需要什么。

请先阅读并遵守：

```text
AGENTS.md
```

特别注意：

- 不要写注释。
- 写好代码后不要执行 `pnpm build`，除非用户单独明确要求。

不要在本文件维护第二套规则；如需调整项目规范，请修改 `AGENTS.md`。
